[package]
name = "native-lib-paths"
version = "0.1.0"
edition = "2021"
rust-version = "1.90.0"
description = "Locate bundled native runtime libraries (CEF, pdfium) relative to the running executable"
repository = "https://github.com/playtron-os/iced_webview_v2/"
license = "Apache-2.0"

# Intentionally dependency-free: consumers like a PDF viewer need this lookup
# without inheriting the webview stack.
[dependencies]
